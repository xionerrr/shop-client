export enum ROUTES {
  home = 'home',
  settings = 'settings',
  todos = 'todos',
  todo = 'todo/:todoId',
  createTodo = 'todo/create',
  updateTodo = 'todo/update/:todoId',

  auth = 'auth',

  notFound = '*',
}
