import { I_AuthLayoutProps } from './models'
import * as S from './styles'

export const AuthLayout = ({ children }: I_AuthLayoutProps) => {
  return <S.AuthLayout>Auth Layout {children}</S.AuthLayout>
}
